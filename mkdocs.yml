site_name: CRISP SMILES
site_description: CRISP SMILES is a Python library for converting SMILES strings to and from a new syntax that preserves stereochemistry information.
repo_url: https://github.com/denovochem/crisp_smiles
repo_name: denovochem/crisp_smiles

theme:
  name: material
  palette:
    primary: indigo
    accent: indigo
  features:
    - navigation.sections
    - navigation.expand
    - search.suggest

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: google  # or numpy, sphinx
            show_source: true
            show_root_heading: true

nav:
  - Quick Start: index.md
  - Installation: installation.md
  - Getting Started: getting_started.md
  - API Reference: reference.md

markdown_extensions:
  - pymdownx.highlight
  - pymdownx.superfences
  - admonition
  - codehilite

extra_css:
  - stylesheets/extra.css